{
  "name": "ride-matching-web-app",
  "version": "1.0.0",
  "description": "A TypeScript web application for ride matching",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:backend": "cd server && npm run dev",
    "build": "cd server && npx prisma generate && cd .. && tsc && vite build",
    "preview": "vite preview",
    "type-check": "tsc --noEmit",
    "db:migrate": "cd server && npx prisma migrate deploy",
    "db:generate": "cd server && npx prisma generate",
    "vercel-build": "cd server && npx prisma generate && cd .. && npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/bcrypt": "^5.0.2",
    "@types/node": "^20.10.0",
    "@vercel/node": "^3.0.0",
    "prisma": "^6.19.0",
    "typescript": "^5.3.3",
    "vite": "^7.2.1"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "bcrypt": "^5.1.1"
  }
}
